<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Driver Management</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen flex items-center justify-center">
  <div class="w-full max-w-md px-4">
    <!-- Login Card -->
    <div class="bg-white rounded-2xl shadow-2xl p-8">
      <!-- Logo/Header -->
      <div class="text-center mb-8">
        <div class="inline-flex items-center justify-center w-16 h-16 bg-black rounded-full mb-4">
          <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
          </svg>
        </div>
        <h1 class="text-3xl font-bold text-gray-900">Driver Dashboard</h1>
        <p class="text-gray-600 mt-2">Melden Sie sich an, um fortzufahren</p>
      </div>

      <!-- Error Message -->
      <% if (error) { %>
        <div class="mb-6 bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg">
          <%= error %>
        </div>
      <% } %>

      <!-- Login Form -->
      <form action="/login" method="POST">
        <div class="space-y-6">
          <!-- Username -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Benutzername
            </label>
            <input 
              type="text" 
              name="username" 
              required
              autofocus
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-gray-500 focus:border-transparent transition"
              placeholder="admin oder partner"
            />
          </div>

          <!-- Password -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Passwort
            </label>
            <input 
              type="password" 
              name="password" 
              required
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-gray-500 focus:border-transparent transition"
              placeholder="Passwort eingeben"
            />
          </div>

          <!-- Submit Button -->
          <button 
            type="submit"
            class="w-full bg-black text-white py-3 rounded-lg hover:bg-gray-800 font-medium transition shadow-lg hover:shadow-xl"
          >
            Anmelden
          </button>
        </div>
      </form>

      <!-- Demo Credentials -->
      <div class="mt-8 pt-6 border-t border-gray-200">
        <p class="text-sm text-gray-600 text-center mb-3 font-medium">Demo-Zugangsdaten:</p>
        <div class="space-y-2 text-xs">
          <div class="bg-blue-50 p-3 rounded-lg">
            <p class="font-semibold text-blue-900">Admin</p>
            <p class="text-blue-700">Username: <code class="bg-blue-100 px-2 py-1 rounded">admin</code></p>
            <p class="text-blue-700">Password: <code class="bg-blue-100 px-2 py-1 rounded">admin123</code></p>
          </div>
          <div class="bg-green-50 p-3 rounded-lg">
            <p class="font-semibold text-green-900">Partner</p>
            <p class="text-green-700">Username: <code class="bg-green-100 px-2 py-1 rounded">partner</code></p>
            <p class="text-green-700">Password: <code class="bg-green-100 px-2 py-1 rounded">partner123</code></p>
          </div>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <p class="text-center text-gray-600 text-sm mt-6">
      © 2025 Driver Management Dashboard
    </p>
  </div>
</body>
</html>
